import { EventEmitter } from '../../../stencil-public-runtime';
interface HowItWorksProps {
    signUp: EventEmitter;
}
export declare const HowItWorks: ({ signUp }: HowItWorksProps) => any;
export {};
